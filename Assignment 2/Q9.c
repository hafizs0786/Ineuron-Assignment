#include<stdio.h>

int main()
{   
    int a;
    float b;
    char c;
    double d;
    printf("Size of int type variable is %d bytes\n",sizeof(a));
    printf("Size of float type variable is %d bytes\n",sizeof(b));
    printf("Size of char type variable is %d byte\n",sizeof(c));
    printf("Size of double type variable is %d bytes\n",sizeof(d));
    return 0;    
}